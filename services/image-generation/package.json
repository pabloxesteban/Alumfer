{
  "name": "@alumfer/image-generation",
  "version": "0.1.0",
  "private": true,
  "description": "Generación de imágenes fotorrealistas para el pipeline de contenido de Alumfer. Claude dirige (prompt); GPT Image genera.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "alumfer-image": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "generate": "npm run build --silent && node dist/cli.js",
    "example": "npm run build --silent && node dist/example.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  },
  "peerDependencies": {
    "sharp": "^0.33.0"
  },
  "peerDependenciesMeta": {
    "sharp": {
      "optional": true
    }
  }
}
